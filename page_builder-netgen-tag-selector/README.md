## Page Builder NetGen tag_selector attribute

### Goal
Add a block 'attribute' that allows selecting netgen tag and saving

The service is registered via .yaml
This allows the 'tag_selector' attribute